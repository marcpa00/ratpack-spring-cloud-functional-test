spring:
  application:
    name: ratpack-spring-cloud-functional-test
  main:
    web-environment: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 5050
